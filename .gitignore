# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Local working notes / experiments
scratch/
*.local.md
*.local.txt

# Build/agent metadata & scratch artifacts (never part of the skill)
PROGRESS.md
.sie/
scratchpad/
*.log

# Secrets / credentials (never commit)
secrets/
.env
!.env.example
*.key
*.pem
*.cred

# NEVER commit user exports or tokens
exports/
all/
all_json/
organized/
organized_json/
*.html
*.json
!.claude-plugin/plugin.json
!skills/**/*.json
# the data-boundary manifest must be tracked, or the gate is a silent no-op on CI/fresh clones
!.dataclass.json
channels.txt
config.json
token*.txt
*.token

# DCE binary (downloaded at runtime, never bundled)
bin/
cli/
DiscordChatExporter.Cli*
*.zip
